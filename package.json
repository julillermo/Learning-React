{
  "name": "learning-react",
  "version": "0.0.1",
  "description": "A respository separate from my work BitBucket where I can just experiment with React",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm --filter=react-playground dev",
    "build": "pnpm --filter=react-playground build",
    "start": "pnpm --filter=react-playground preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
